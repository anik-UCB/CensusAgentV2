version: 1
artifacts:
  - path: CENSUS_SV.sv.yaml
    type: semantic_view
    targets:
      default:
        object: CENSUS_AGENT.PUBLIC.CENSUS_SV
  - path: CENSUS_CHAT_AGENT.agent.yaml
    type: cortex_agent
    targets:
      default:
        object: CENSUS_AGENT.PUBLIC.CENSUS_CHAT_AGENT
